version: 0.0.3
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install && pip install -r requirements.txt
icon: ./icon.png
dependencies:
  {}
name: oomol-transform
displayName: Transform
description: Common data conversion.
releaseNotes: this is the first version.
